import * as React from "react";
import { hot } from "react-hot-loader";
import "./App.css";

const App = () => {
  return (
    <div className="app">
      <h1>Hello, world!</h1>
    </div>
  );
};

export default hot(module)(App);
